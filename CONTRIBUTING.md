# Внесок в Shiftly

Дякуємо за інтерес до Shiftly! Ми раді будь-якому внеску.

## Як допомогти

### Повідомлення про помилки

Якщо ви знайшли помилку:

1. Перевірте, чи не існує вже подібного issue
2. Створіть новий issue з деталями:
   - Опис проблеми
   - Кроки для відтворення
   - Очікувана поведінка
   - Фактична поведінка
   - Скріншоти (якщо доречно)
   - Середовище (ОС, версія Node.js, тощо)

### Пропозиції нових функцій

1. Створіть issue з тегом "enhancement"
2. Опишіть функцію та її користь
3. Обговоріть з командою перед початком розробки

### Внесення коду

1. Fork репозиторій
2. Створіть нову гілку: `git checkout -b feature/amazing-feature`
3. Внесіть зміни
4. Перевірте код:
   ```bash
   # Backend
   cd backend
   npm run build

   # Frontend
   cd frontend
   npm run build
   ```
5. Commit змін: `git commit -m 'Add amazing feature'`
6. Push до гілки: `git push origin feature/amazing-feature`
7. Створіть Pull Request

## Стандарти коду

### TypeScript

- Використовуйте TypeScript для всього коду
- Уникайте `any` типів
- Додавайте типи для всіх параметрів та повернутих значень

### Іменування

- **Файли:** camelCase або kebab-case
- **Компоненти:** PascalCase
- **Функції/змінні:** camelCase
- **Константи:** UPPER_SNAKE_CASE

### Коментарі

- Пишіть зрозумілий код, що не потребує багато коментарів
- Додавайте коментарі для складної логіки
- Використовуйте JSDoc для публічних API

## Структура Commit повідомлень

```
<type>: <subject>

<body>

<footer>
```

**Types:**
- `feat`: Нова функція
- `fix`: Виправлення помилки
- `docs`: Зміни в документації
- `style`: Форматування коду
- `refactor`: Рефакторинг
- `test`: Додавання тестів
- `chore`: Оновлення залежностей, тощо

**Приклад:**
```
feat: add drag-and-drop for schedule

Implemented drag-and-drop functionality using @dnd-kit
that allows users to easily move shifts between days.

Closes #123
```

## Процес Review

1. Всі PR переглядаються maintainer-ами
2. Отримайте принаймні 1 схвалення
3. Пройдіть всі перевірки CI/CD
4. Вирішіть всі коментарі
5. Merge виконується через squash

## Розробка

### Налаштування середовища

Див. [SETUP.md](SETUP.md) для деталей

### Запуск тестів

```bash
# Backend
cd backend
npm test

# Frontend
cd frontend
npm test
```

### Локальна база даних

Використовуйте Docker для локального PostgreSQL:

```bash
docker run --name shiftly-postgres -e POSTGRES_PASSWORD=password -p 5432:5432 -d postgres:14
```

## Пріоритети

Поточні пріоритети (в порядку важливості):

1. Drag-and-drop для розкладу
2. Експорт в PDF/Excel
3. Email сповіщення
4. Мобільний додаток
5. Розширена аналітика

## Запитання?

- Створіть issue з тегом "question"
- Напишіть в Discussions
- Зв'яжіться з maintainer-ом

## Код Поведінки

- Будьте шанобливі
- Приймайте конструктивну критику
- Фокусуйтеся на тому, що найкраще для проекту
- Допомагайте новачкам

Дякуємо за ваш внесок! 🎉
